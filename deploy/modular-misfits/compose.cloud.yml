services:
  gateway:
    environment:
      LEGACY_DOCUMENSO_UPSTREAM: documenso:3000
    networks:
      - default
      - legacy_documenso

networks:
  legacy_documenso:
    external: true
    name: modular-misfits-esign-shared
